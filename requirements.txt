# ChandraOCR Batch Inference Dependencies
# Note: Install the modified mlx-vlm separately with: pip install -e mlx-vlm-0.3.10/

# Core dependencies (these will also be installed via mlx-vlm)
mlx>=0.30.0
mlx-lm>=0.30.5
transformers>=5.0.0rc3
Pillow>=10.3.0
tqdm>=4.66.2
numpy
